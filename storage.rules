rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default deny all access to storage
    // Uncomment and modify the rules below if you plan to use Firebase Storage

    // Example: User profile pictures
    // match /profile_pictures/{userId}/{fileName} {
    //   allow read: if true; // Public read
    //   allow write: if request.auth != null
    //     && request.auth.uid == userId
    //     && request.resource.size < 5 * 1024 * 1024 // Max 5MB
    //     && request.resource.contentType.matches('image/.*'); // Images only
    // }

    // Example: Game assets (if you want to store plane skins, etc.)
    // match /game_assets/{assetPath=**} {
    //   allow read: if true; // Public read for game assets
    //   allow write: if false; // Only admins can write via console/functions
    // }

    // Deny all by default
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
