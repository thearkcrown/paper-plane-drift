{
  "name": "paper-plane-drift",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "firebase:deploy": "npm run build && firebase deploy",
    "firebase:deploy:rules": "firebase deploy --only firestore:rules,storage",
    "firebase:deploy:hosting": "npm run build && firebase deploy --only hosting",
    "firebase:emulators": "firebase emulators:start",
    "firebase:test": "firebase emulators:exec --only firestore 'npm test'"
  },
  "dependencies": {
    "firebase": "^12.6.0",
    "lucide-react": "^0.554.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "firebase-tools": "^13.35.1",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
